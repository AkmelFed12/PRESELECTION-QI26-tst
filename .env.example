# ============================================================
# Variables d'environnement — Quiz Islamique 2026
# Copier ce fichier en .env et remplir les valeurs
# ============================================================

# Base de données (par défaut local: postgresql://localhost:5432/quiz26)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/quiz26

# Admin (identifiants par défaut)
ADMIN_USERNAME=asaa2026
ADMIN_PASSWORD=ASAALMO2026

# Optionnel — contact admin
ADMIN_WHATSAPP=2250150070083

# Optionnel — Cloudinary pour photos candidats
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=quiz-islamique

# Optionnel — notifications email (inscription + contact)
# Si configuré, l'admin reçoit un email à chaque nouvelle inscription
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ouattaral2@student.iugb.edu.ci
SMTP_PASSWORD=
SMTP_FROM=noreply@quizislamique.com
SMTP_TO=ouattaral2@student.iugb.edu.ci

# Optionnel — port (défaut: 3000)
PORT=3000
