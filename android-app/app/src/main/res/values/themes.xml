<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Utilisation d'un thème plus compatible (MaterialComponents) -->
    <style name="Theme.ASAA" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleur de la barre de statut -->
        <item name="android:statusBarColor">@android:color/black</item>
        <!-- Les options de SplashScreen spécifiques à Android 12+ doivent être enlevées d'ici -->
    </style>
</resources>
